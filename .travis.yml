sudo: false
language: ruby
cache: bundler
branches:
  only:
    - master

before_install:
  - gem update --system
  - gem install bundler

rvm:
  - 2.4.3

script:
  - bundle install --jobs=3 --retry=3
  - bundle exec rake build
  - bundle exec rake driver:cycle
